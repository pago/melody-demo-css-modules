{% from './counter.css' import header %}
<div>
  <h3 class="{{ header }}">Counter: {{ count }}</h3>
  <button ref="{{ incrementButton }}"> + </button>
  <button ref="{{ decrementButton }}"> - </button>
</div>